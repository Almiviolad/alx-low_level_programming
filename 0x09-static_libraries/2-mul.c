#include "main.h"
/**
 *mul-multiplies 2 numbers
 *@a:first number to be multiply
 *@b:second number to be multiplied
 *Return:result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
