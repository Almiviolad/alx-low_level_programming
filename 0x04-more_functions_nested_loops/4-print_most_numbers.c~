#include "main.h"
/**
 *print_most_numbers - prints number 0 through 9 except 2 and 4
 *
 *Description: prints 0 through 9 using the _putchar function except 2 and 4 
 */
void print_most_numbers(void)
{
	char i;

	for (i = 48; i <= 57; i++)
	{
		if ((i !=50) && (i != 52))
			_putchar(i);
	}
	_putchar('\n');
}
