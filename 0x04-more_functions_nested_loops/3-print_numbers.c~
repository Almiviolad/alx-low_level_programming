#include "main.h"
/**
 *print_numbers - prints number 0 through 9
 *
 *Description: prints 0 through 9 using the _putchar function
 */
void print_numbers(void)
{
	char i;
	
	for (i = 48; i <= 57; i++)
	{
		_putchar(i);
	}
}
