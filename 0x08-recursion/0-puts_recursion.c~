#include "main.h"
/**
 *_puts_recursíon - prints a string followed by nre line
 *@s: the string pointer
 */
void _puts_recursion(char *s)
{
	if (*s == '\0')
	{
		putchar('\n');
	}
	else
	{
		putchar(*s);
		_puts_recursion(s + 1);
	}
}
