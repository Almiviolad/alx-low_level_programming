#include <stdio.h>
/**
 *main - entry point
 *@argc: number of arguments
 *@argv: array containg the argumnets
 *Return: always 0(sucess)
 */
int main(int argc, const char *argv[])
{
	(void)argc;
	printf("%s", argv[0]);
	return (0);
}
