#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main-Entry point
 * Description: To determine if number is greater\
,less or equal to zero
 * Return:Always 0 (success)
*/

int main(void)
{
	int a,b;

	for (a = 48; a <= 57; a++)
		putchar(a);
	for (b = 97; b <= 102; b++)
		putchar(b);
	putchar('\n');
	return (0);
}
