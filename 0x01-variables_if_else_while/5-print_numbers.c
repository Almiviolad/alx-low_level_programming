#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main-Entry point
 * Description: To determine if number is greater\
,less or equal to zero
 * Return:Always 0 (success)
*/

int main(void)
{
	int a;

	for (a = 0 ; a < 10; a++)
		printf("%d", a);
	return (0);
}
